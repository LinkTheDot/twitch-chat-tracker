#!/usr/bin/env zsh

docker build --progress=plain -t $HOMELAB_ADDRESS:5000/twitch-logger-frontend:latest -f frontend/Dockerfile frontend;

docker push $HOMELAB_ADDRESS:5000/twitch-logger-frontend:latest

